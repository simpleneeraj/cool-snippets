

interface SampleType {
  id: number;
  str: string;
}

const samplecode: SampleType[] = [
  {
    id: 0,
    str:
      `
if (tweeted || facebooked) {
  gratitude += 1;
}
if (githubStarred) {
  karmaBalance = true;
} else {
  $('we').say('ok');
}
`.trim()
  },
  {
    id: 1,
    str: `
while(true){
  // i love you
}
`.trim()
  }
]

export default samplecode;

/**
<!-- --> <-- ->> <<- -> <- => <=> <==> ==> <== 
>>= =<< -- := =:= == !== != <= >= // && .& 
|| !! :: >> << ¯\_(ツ)_/¯ __ ___ .. ...
*/

/**

-<< -< -<- <-- <--- <<- <- -> ->> --> ---> ->- >- >>-
=<< =< =<= <== <=== <<= <= => =>> ==> ===> =>= >= >>=
<-> <--> <---> <----> <=> <==> <===> <====> -------->
<~~ <~ ~> ~~> :: ::: == != /= ~= <> === !== =/= =!=
:= :- :+ <* <*> *> <| <|> |> <. <.> .> +: -: =: :> __
(* *) [| |] {| |} ++ +++ \/ /\ |- -| <!-- <!--- <***>

 */
