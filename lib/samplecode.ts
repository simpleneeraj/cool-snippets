

const samplecode = [
    {
        id: 0,
        str: `if (tweeted || facebooked) {
            gratitude += 1;
          }
          if (githubStarred) {
            karmaBalance = true;
          } else {
            $('we').say('ok');
          }`
    }
]

export default samplecode;

// Can be turned off <!-- --> <-- ->> <<- -> <- => <=> <==> ==> <== >>= =<< -- := =:= == !== != <= >= // /** /* */ && .& || !! :: >> << ¯\_(ツ)_/¯ __ ___ .. ...