import React from 'react';
import PricingClient from './client';

type ChangelogPageProps = object;

const PricingPage: React.FC<ChangelogPageProps> = ({}) => {
  return <PricingClient />;
};

export default PricingPage;
